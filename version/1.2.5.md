# 1.2.5

## 修改文件

### src/components/TagsPage.tsx
- 删除 Banner 下方的四宫格横向素材条（即截图 `image-1.png` 中那一整排卡片）。
- 保留其余trails页结构与业务逻辑不变：
  - 帖子拉取、搜索过滤、trails统计
  - 访客模式说明（无注册，仅 RobinElysia 与 Meow 管理）
  - 左侧拖拽轴、中央卡组翻转、详情派送联动
- 清理删除区域对应的未使用素材常量，避免冗余代码。

## 校验
- `npm.cmd run lint`：通过（`tsc --noEmit` 无错误）
- `npm.cmd run build`：通过（仅保留 Vite chunk size warning）
