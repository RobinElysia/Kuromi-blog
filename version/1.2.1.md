# 1.2.1

## Changed Files
1. `src/components/Home.tsx`
2. `src/index.css`

## Change Details
1. Scroll rail circle alignment fix
- Removed `transform` from `acrylic-dot` keyframes to avoid overriding the element's translate positioning.
- The draggable circle now stays on the center line instead of rendering beside it.

2. Required project notes added on Home
- Added a new `Project Notes` card in the left panel while keeping the current color palette and background style.
- Notes include:
  - No registration flow.
  - Only `RobinElysia` and `Meow` are admins.
  - Guest mode is available.
  - Visual assets are referenced from `public/kuromi`.
  - Component design follows `shadcn/ui` + `Framer Motion`.

3. Dot rendering stability
- Added `z-10` and `transform-gpu` to the draggable rail dot for more stable rendering and layering.

## Validation
1. `npm.cmd run lint` passed (`tsc --noEmit`).
2. `npm.cmd run build` passed (Vite production build succeeded).
