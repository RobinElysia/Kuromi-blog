# 1.2.10

## 修改文件
- `src/components/TagsPage.tsx`

## 修改内容
1. 修正trails卡片点击后的跳转目标
- 将卡片点击后的动作从“进入帖子详情（`onOpenPost`）”改为“进入对应记忆派送页面（dispatch）”。
- 现在点击卡片后流程为：放大翻转动画 -> 激活对应标签 -> 切换到 `dispatch` 页签。

2. 保留并明确交互链路
- 保留已有卡片放大与翻转动画。
- 保留标签路由同步（`/tags?tag=...`），确保派送列表内容与点击标签一致。

## 保持不变
- 配色、背景、毛玻璃/渐变视觉风格不变。
- 无注册，仅 RobinElysia 与 Meow 管理，访客模式逻辑不变。
- 响应式布局结构不变。

## 本地校验
- `npm run lint`：通过
- `npm run build`：通过（存在项目既有 chunk size warning，非本次改动引入）
