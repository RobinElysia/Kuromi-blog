# 1.2.4

## 修改文件

### src/components/TagsPage.tsx
- 保留并复用原有业务逻辑：
  - 帖子拉取（`/api/posts?limit=30`）
  - 搜索过滤
  - trails统计
  - URL `tag` 参数同步
  - trails筛选与帖子详情跳转
- 修复原文件中文乱码，统一为正常 UTF-8 文本。
- 按新需求重构“trails页旧史”骨架：
  - 左侧新增可拖拽滚动轴（drag axis），拖动可改变trails焦点。
  - 中央改为扇形卡组布局，卡片支持鼠标聚焦上浮（hover lift）。
  - 点击卡片触发 3D 翻转动画，动画结束后自动切换到“记忆派送”并定位对应trails。
- 视觉与交互新增：
  - 毛玻璃、渐变光层、粒子感漂浮点、阴影与景深透视。
  - 骨架屏加载态与弹性动画。
  - 保持现有配色/背景体系，继续使用 shadcn/radix 组件 + Framer Motion + Animate.css。
- 项目说明文案继续明确：
  - 无注册入口，仅 RobinElysia 与 Meow 可管理。
  - 访客模式可浏览内容。
- 响应式处理：
  - 移动端与桌面端均可用；动效在 `prefers-reduced-motion` 下自动降级。

## 校验
- `npm.cmd run lint`：通过（`tsc --noEmit` 无错误）
- `npm.cmd run build`：通过（仅保留 Vite 大包体积 warning）
